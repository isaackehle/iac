# PostgreSQL — Docker Compose Stack
#
# Stack version: 1.0
# Purpose: PostgreSQL database server with pgAdmin interface
#
# Services:
#   - db (PostgreSQL database)
#   - pgadmin (pgAdmin web interface)
#
# Environment variables:
#   - STACK_NAME=PostgreSQL
#   - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
#   - PGADMIN_DEFAULT_EMAIL=${PGADMIN_DEFAULT_EMAIL}
#   - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_DEFAULT_PASSWORD}
#   See .env.example for all required variables
#
# Deployment:
#   docker compose -f postgresql.yaml up -d
#   docker compose -f postgresql.yaml down
#
name: PostgreSQL
version: "3.8"

services:
  # ── Identity ──────────────────────────────────────────────
  db:
    image: postgres
    container_name: PostgreSQL

    # ── Lifecycle ─────────────────────────────────────────────
    restart: on-failure:5

    # ── Network ───────────────────────────────────────────────
    ports:
      - "2665:5454"               # PostgreSQL port (mapped from 5432)

    # ── Storage ───────────────────────────────────────────────
    volumes:
      - /volume1/docker/stacks/postgresql:/var/lib/postgresql:rw

    # ── Environment ───────────────────────────────────────────
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=thunder_db

    # ── Runtime ───────────────────────────────────────────────
    healthcheck:
      test: ["CMD", "pg_isready", "-q", "-d", "thunder_db", "-U", "root"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 256m
    cpu_shares: 768

  # ── Identity ──────────────────────────────────────────────
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgAdmin

    # ── Lifecycle ─────────────────────────────────────────────
    restart: on-failure:5

    # ── Network ───────────────────────────────────────────────
    ports:
      - "2660:5050"               # pgAdmin web interface

    # ── Storage ───────────────────────────────────────────────
    volumes:
      - /volume1/docker/stacks/postgresql/pgadmin:/var/lib/pgadmin:rw

    # ── Environment ───────────────────────────────────────────
    environment:
      - PGADMIN_DEFAULT_EMAIL=${PGADMIN_DEFAULT_EMAIL}
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_DEFAULT_PASSWORD}
      - PGADMIN_LISTEN_PORT=5050

    # ── Runtime ───────────────────────────────────────────────
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:5050"]
      interval: 30s
      timeout: 10s
      retries: 3
    mem_limit: 256m
    cpu_shares: 768
    user: "0:0"
