repos:
  - repo: local
    hooks:
      - id: rumdl
        name: rumdl lint
        description: Lint markdown files with rumdl
        entry: rumdl
        args: [--config, .rumdl.toml, fmt]
        language: system
        files: \.md$
        pass_filenames: false
        require_serial: true
