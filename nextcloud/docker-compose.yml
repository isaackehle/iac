# Nextcloud — Docker Compose Stack
#
# Stack version: 1.0
# Purpose: Personal cloud storage and collaboration platform
#
# Services:
#   - nextcloud
#   - tailscale-sidecar (Tailscale sidecar)
#   - db (PostgreSQL database)
#   - redis (Redis cache)
#
# Environment variables:
#   - STACK_NAME=Nextcloud
#   - NC_DB_PASSWORD=${NC_DB_PASSWORD}
#   - NC_ADMIN_USER=${NC_ADMIN_USER}
#   - NC_ADMIN_PASSWORD=${NC_ADMIN_PASSWORD}
#   - TS_AUTHKEY=${TS_AUTHKEY}
#   - TS_CERT_DOMAIN=${TS_CERT_DOMAIN}
#   See .env.example for all required variables
#
# Deployment:
#   docker compose -f nextcloud.yaml up -d
#   docker compose -f nextcloud.yaml down
#
# Networking (Pattern B — Tailscale sidecar):
#   tailscale-sidecar owns the network namespace and joins nextcloud-net.
#   nextcloud borrows the sidecar's netns (network_mode: service:) so the
#   sidecar's serve.json can proxy to the app over 127.0.0.1, exactly like
#   openwebui/n8n. db and redis are siblings on nextcloud-net; nextcloud
#   resolves them by name through the sidecar's membership on that network.
#

name: nextcloud

services:
  # ── Identity ──────────────────────────────────────────────
  tailscale-sidecar:
    image: tailscale/tailscale:latest
    container_name: ts-nextcloud

    # ── Lifecycle ─────────────────────────────────────────────
    restart: unless-stopped

    # ── Network ───────────────────────────────────────────────
    # Owns the netns; joins nextcloud-net so borrowers can resolve db/redis.
    networks:
      - nextcloud-net

    # ── Storage ───────────────────────────────────────────────
    volumes:
      - /volume1/docker/stacks/nextcloud/ts-config:/config
      - /volume1/docker/stacks/nextcloud/ts-state:/var/lib/tailscale

    # ── Environment ───────────────────────────────────────────
    environment:
      - TS_HOSTNAME=nextcloud
      - TS_AUTHKEY=${TS_AUTHKEY}
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_AUTH_ONCE=true
      - TS_SERVE_CONFIG=/config/serve.json

    # ── Runtime ───────────────────────────────────────────────
    cap_add:
      - NET_ADMIN
      - NET_RAW
    devices:
      - /dev/net/tun:/dev/net/tun

  # ── Identity ──────────────────────────────────────────────
  nextcloud:
    image: nextcloud:apache
    container_name: nextcloud

    # ── Lifecycle ─────────────────────────────────────────────
    restart: unless-stopped
    depends_on:
      - tailscale-sidecar
      - db
      - redis

    # ── Network ───────────────────────────────────────────────
    # Borrows the sidecar's netns; no ports/networks of its own.
    network_mode: service:tailscale-sidecar

    # ── Storage ───────────────────────────────────────────────
    volumes:
      - /volume1/docker/stacks/nextcloud/app:/var/www/html
      - /volume1/docker/stacks/nextcloud/data:/var/www/html/data

    # ── Environment ───────────────────────────────────────────
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_DB=nextcloud
      - POSTGRES_USER=nextcloud
      - POSTGRES_PASSWORD=${NC_DB_PASSWORD}
      - REDIS_HOST=redis
      - NEXTCLOUD_ADMIN_USER=${NC_ADMIN_USER}
      - NEXTCLOUD_ADMIN_PASSWORD=${NC_ADMIN_PASSWORD}
      - NEXTCLOUD_TRUSTED_DOMAINS=${TS_CERT_DOMAIN}
      - OVERWRITEPROTOCOL=https
      - OVERWRITECLIURL=https://${TS_CERT_DOMAIN}
      - PHP_MEMORY_LIMIT=1G
      - PHP_UPLOAD_LIMIT=10G

  # ── Identity ──────────────────────────────────────────────
  db:
    image: postgres:16
    container_name: nextcloud-db

    # ── Lifecycle ─────────────────────────────────────────────
    restart: unless-stopped

    # ── Network ───────────────────────────────────────────────
    networks:
      - nextcloud-net

    # ── Storage ───────────────────────────────────────────────
    volumes:
      - /volume1/docker/stacks/nextcloud/postgres:/var/lib/postgresql/data

    # ── Environment ───────────────────────────────────────────
    environment:
      - POSTGRES_DB=nextcloud
      - POSTGRES_USER=nextcloud
      - POSTGRES_PASSWORD=${NC_DB_PASSWORD}

  # ── Identity ──────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: nextcloud-redis

    # ── Lifecycle ─────────────────────────────────────────────
    restart: unless-stopped

    # ── Network ───────────────────────────────────────────────
    networks:
      - nextcloud-net

networks:
  nextcloud-net:
    driver: bridge
